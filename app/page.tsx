import { useEffect, useMemo, useRef, useState } from 'react';
import { ArrowLeft, ChevronRight, Eye, Fingerprint, LockKeyhole, Volume2, VolumeX } from 'lucide-react';

type Gender = 'male' | 'female';
type FunctionKey = 'Se'|'Si'|'Ne'|'Ni'|'Te'|'Ti'|'Fe'|'Fi';
type Stage = 'boot'|'register'|'assessment'|'reveal'|'dossier';

const asset=(path:string)=>`${import.meta.env.BASE_URL}${path}`;
const stacks:Record<string,FunctionKey[]>={ISTJ:['Si','Te','Fi','Ne'],ISFJ:['Si','Fe','Ti','Ne'],INFJ:['Ni','Fe','Ti','Se'],INTJ:['Ni','Te','Fi','Se'],ISTP:['Ti','Se','Ni','Fe'],ISFP:['Fi','Se','Ni','Te'],INFP:['Fi','Ne','Si','Te'],INTP:['Ti','Ne','Si','Fe'],ESTP:['Se','Ti','Fe','Ni'],ESFP:['Se','Fi','Te','Ni'],ENFP:['Ne','Fi','Te','Si'],ENTP:['Ne','Ti','Fe','Si'],ESTJ:['Te','Si','Ne','Fi'],ESFJ:['Fe','Si','Ne','Ti'],ENFJ:['Fe','Ni','Se','Ti'],ENTJ:['Te','Ni','Se','Fi']};
const aliases:Record<string,string>={INTJ:'社会达尔文屠夫',INTP:'无底线观测者',ENTJ:'马基雅维利暴君',ENTP:'虚无主义煽动家',INFJ:'精神邪教主',INFP:'受害者暴君',ENFJ:'认知寄生母体',ENFP:'狂热毒源体',ISTJ:'极权执行官',ISFJ:'窒息施恩者',ESTJ:'齿轮独裁者',ESFJ:'蜂巢审判长',ISTP:'冰冷解剖者',ISFP:'绝望美学家',ESTP:'掠食资本家',ESFP:'痛觉狂欢客'};
const questions:{code:string,scene:string,a:{text:string,fn:FunctionKey},b:{text:string,fn:FunctionKey}}[]=[
{code:'P-01',scene:'陌生房间里只留下了一分钟。你首先会记住什么？',a:{text:'眼前物品的位置、声音与可以立刻利用的出口',fn:'Se'},b:{text:'它与我经历过的某个地方有哪些细节重合',fn:'Si'}},
{code:'P-02',scene:'一份缺失了三页的案件记录摆在面前。',a:{text:'同时列出数种可能，寻找意外连接',fn:'Ne'},b:{text:'压缩所有线索，寻找唯一贯穿始终的意图',fn:'Ni'}},
{code:'J-01',scene:'团队必须在今晚决定牺牲哪一个方案。',a:{text:'比较成本、速度和可以被验证的实际成果',fn:'Te'},b:{text:'先检查规则本身是否自洽，再接受结论',fn:'Ti'}},
{code:'J-02',scene:'一个人承认犯错，却拒绝解释原因。',a:{text:'观察这件事如何影响所有人的关系与情绪',fn:'Fe'},b:{text:'判断他的行为是否背叛了自己真正相信的东西',fn:'Fi'}},
{code:'P-03',scene:'监控画面只出现了半秒钟的异常。',a:{text:'相信当时直接看到的动作与物理变化',fn:'Se'},b:{text:'回查此前每一次相似记录，寻找稳定偏差',fn:'Si'}},
{code:'P-04',scene:'两名证人给出了完全相反的说法。',a:{text:'保留矛盾，让更多解释同时存在',fn:'Ne'},b:{text:'寻找他们都没有说出口的共同前提',fn:'Ni'}},
{code:'J-03',scene:'制度没有禁止一件明显会伤害人的事。',a:{text:'立即补上可执行的流程与责任人',fn:'Te'},b:{text:'先定义问题，避免用错误规则制造新漏洞',fn:'Ti'}},
{code:'J-04',scene:'公开真相会让无辜者承受长期羞辱。',a:{text:'先考虑群体将如何反应以及如何降低伤害',fn:'Fe'},b:{text:'即使不被理解，也不违背个人的道德底线',fn:'Fi'}},
{code:'S-01',scene:'压力下，你发现计划正在失控。',a:{text:'进入现场，边行动边修正判断',fn:'Se'},b:{text:'回到已验证的方法，一项项恢复秩序',fn:'Si'}},
{code:'S-02',scene:'有人暗示你正在参与一项更大的计划。',a:{text:'追踪每一种可能扩散出去的后果',fn:'Ne'},b:{text:'直觉上锁定那个最可能的最终目的',fn:'Ni'}},
{code:'S-03',scene:'所有人都等待你承担责任。',a:{text:'接管资源并给出明确指令',fn:'Te'},b:{text:'拒绝仓促表态，独自重算所有因果',fn:'Ti'}},
{code:'S-04',scene:'一个重要的人请求你隐瞒他的过错。',a:{text:'衡量关系、信任和其他人的承受能力',fn:'Fe'},b:{text:'先问自己是否愿意成为这种行为的一部分',fn:'Fi'}}];

export default function Home(){
const[stage,setStage]=useState<Stage>('boot');const[gender,setGender]=useState<Gender>('female');const[index,setIndex]=useState(0);const[answers,setAnswers]=useState<FunctionKey[]>([]);const[muted,setMuted]=useState(false);const[noticed,setNoticed]=useState(0);const card=useRef<HTMLDivElement>(null);
useEffect(()=>{document.title='观察者登记｜16'},[]);
const scores=useMemo(()=>{const s={} as Record<FunctionKey,number>;(['Se','Si','Ne','Ni','Te','Ti','Fe','Fi'] as FunctionKey[]).forEach(k=>s[k]=0);answers.forEach(k=>s[k]++);return s},[answers]);
const type=useMemo(()=>Object.entries(stacks).map(([name,stack])=>({name,value:stack.reduce((sum,fn,i)=>sum+scores[fn]*[4,3,2,1][i],0)})).sort((a,b)=>b.value-a.value)[0]?.name||'INTJ',[scores]);
const stack=stacks[type];
const choose=(fn:FunctionKey)=>{const next=[...answers,fn];setAnswers(next);if(index===questions.length-1){setStage('reveal');setTimeout(()=>setNoticed(1),1800)}else setIndex(index+1)};
const moveEyes=(clientX:number,clientY:number)=>{if(!card.current||stage!=='reveal')return;const r=card.current.getBoundingClientRect();const dx=Math.max(-1,Math.min(1,(clientX-(r.left+r.width/2))/(r.width/2)));const dy=Math.max(-1,Math.min(1,(clientY-(r.top+r.height*.42))/(r.height/2)));card.current.style.setProperty('--eye-x',`${dx*5}px`);card.current.style.setProperty('--eye-y',`${dy*3}px`);if(noticed===1&&Math.abs(dx)>.28)setNoticed(2)};
const portrait=asset(`portraits/${type.toLowerCase()}-${gender}.png`);
return <main className={`shell stage-${stage}`} onPointerMove={e=>moveEyes(e.clientX,e.clientY)} onPointerDown={e=>moveEyes(e.clientX,e.clientY)}><div className="grain"/><header><div className="wordmark"><Fingerprint size={18}/><span>PRISM / XVI</span></div><div className="sys-state"><i/> INTERNAL NETWORK</div><button className="sound" onClick={()=>setMuted(!muted)} aria-label={muted?'打开声音':'静音'}>{muted?<VolumeX/>:<Volume2/>}</button></header>
{stage==='boot'&&<section className="boot enter"><div className="boot-log"><span>PRISM BEHAVIORAL ARCHIVE</span><span>SECURE CHANNEL ESTABLISHED</span><span>IDENTITY: UNRESOLVED</span></div><div className="boot-mark"><Eye/><p>本终端将记录你的判断方式，而不是答案本身。</p></div><button className="action" onClick={()=>setStage('register')}><LockKeyhole size={17}/> 申请临时阅档权限</button><small>继续即代表你知悉：观察行为本身将成为档案的一部分。</small></section>}
{stage==='register'&&<section className="register enter"><button className="back" onClick={()=>setStage('boot')}><ArrowLeft/> 返回</button><div className="terminal-label">OPERATOR REGISTRATION</div><p className="prompt">选择系统用于匹配镜像样本的外观档案。</p><div className="gender-pick"><button className={gender==='female'?'active':''} onClick={()=>setGender('female')}><img src={asset('mbti-women.jpg')} alt="女性样本总表"/><span>女性镜像样本</span></button><button className={gender==='male'?'active':''} onClick={()=>setGender('male')}><img src={asset('mbti-men.jpg')} alt="男性样本总表"/><span>男性镜像样本</span></button></div><div className="notice"><b>认知功能取证</b><p>接下来将比较 Se / Si / Ne / Ni / Te / Ti / Fe / Fi 的行为偏好。没有正确选项。</p></div><button className="action" onClick={()=>setStage('assessment')}>开始登记 <ChevronRight/></button></section>}
{stage==='assessment'&&<section className="assessment enter" key={index}><div className="question-top"><button className="back" onClick={()=>index?(setIndex(index-1),setAnswers(answers.slice(0,-1))):setStage('register')}><ArrowLeft/> 撤回</button><span>{String(index+1).padStart(2,'0')} / {questions.length}</span></div><div className="meter"><i style={{width:`${((index+1)/questions.length)*100}%`}}/></div><div className="terminal-label">EVIDENCE {questions[index].code}</div><h1>{questions[index].scene}</h1><div className="answers"><button onClick={()=>choose(questions[index].a.fn)}><b>A</b><span>{questions[index].a.text}</span></button><button onClick={()=>choose(questions[index].b.fn)}><b>B</b><span>{questions[index].b.text}</span></button></div><p className="footnote">系统同时记录停留时间、撤回答案与选择顺序。</p></section>}
{stage==='reveal'&&<section className="reveal enter"><div className="match-line">FUNCTION SEQUENCE MATCHED</div><p className="your-type">您的人格是：</p><div className={`live-card ${noticed?'awakened':''}`} ref={card}><img src={portrait} alt={`${type} 单人证件照`}/><div className="screen-lines"/><div className="eye left"><i/></div><div className="eye right"><i/></div><div className="id-strip"><span>SUBJECT XVI-{type}</span><b>{type}</b><em>{aliases[type]}</em></div></div><div className="function-stack">{stack.map((fn,i)=><span key={fn}><small>{['主导','辅助','第三','劣势'][i]}</small>{fn}</span>)}</div><div className={`system-alert level-${noticed}`}>{noticed===0?'正在调取受试者视觉记录…':noticed===1?'视觉反馈通道异常。请移动指针确认。':'受试者已确认观察者位置。'}</div>{noticed===2&&<button className="action danger" onClick={()=>setStage('dossier')}>打开受限档案 <ChevronRight/></button>}</section>}
{stage==='dossier'&&<section className="dossier enter"><div className="access-row"><span>ACCESS LEVEL 01</span><b>已解锁 1 / 32</b></div><div className="redaction"><span>内部复核通知</span><p>系统在十一年前已将该受试者标记为死亡。</p><p>当前照片的视觉反馈并非来自原始影像文件。</p><strong>请确认：你是否仍要继续观察？</strong></div><div className="evidence-file"><div><small>异常记录</small><b>LIVE DOSSIER / 00:16:33</b></div><p>在你移动指针前，系统已经提前生成了受试者下一次注视的位置。</p><p className="typed">分析对象可能不是照片。</p></div><button className="action locked"><LockKeyhole/> 后续档案正在封存</button><button className="restart" onClick={()=>{setStage('boot');setIndex(0);setAnswers([]);setNoticed(0)}}>清除本次观察记录</button></section>}
</main>}
